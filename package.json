{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "turbo": "^1.13.0",
    "vite": "^5.2.2"
  }
}
